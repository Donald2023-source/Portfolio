{
    "version": 2,
    "builds": [
        {
            "src": "src/**/*.jsx",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/src/api/$1.js"
        }
    ]
}